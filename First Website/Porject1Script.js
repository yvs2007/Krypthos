document.getElementsByClassName('.SubmitButton').addEventListener('click', function() {
    // When the button is clicked, store the button input in a variable
    var buttonInput = "Button Clicked";
    
    // You can now use the variable buttonInput to do whatever you need with the input
    console.log('Button Input:', buttonInput);
});